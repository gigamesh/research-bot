/// `__BUILD_MARKER__` is injected by Vite via `define` (see vite.config.ts).
/// ISO timestamp of the build, used as a load-time identity check so a
/// stale service worker / content script can be detected at a glance.
declare const __BUILD_MARKER__: string;
